int wain(int a, int b){
  int start = 0;
  int end = 0;
  int counter = 0;
  if(a >= b){
    start = b;
    end = a;
  }
  else{
    start = a;
    end = b;
  }
  while (start <= end){
    counter = counter + start;
    start = start + 1;
  }
  return counter;
}
