int wain(int a, int b){
  int temp = 0;
  int rem = 1;
  if(a < b){
    temp = a;
    a = b;
    b = temp;
  }
  else{
  }
  while(rem != 0){
    rem = a % b;
    a = b;
    if(rem != 0){
      b = rem;
    }
    else{
    }
  }
  return b;
}
